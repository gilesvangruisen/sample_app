<li>
	<%= gravatar_for user %>
	<%= link_to user.name, user %>
	<%= link_to "delete", user, method: :delete, data: { confirm: "You sure?" }, id: 'delete'+user.name if current_user.admin? && !current_user?(user) %>
</li>